// si nombramos dos archivos con el mismo cógido habrá errores
(()=>{
let productName1 = 'Product 1'; // ts puede inferir el tipo de dato en el tooltype
let porductPrice1 = 123;

productName = 'cHange';
productName.toLocaleLowerCase();
porductPrice.toFixed(); // cuando tenemos un tipo de dato no da las opc que se pueden ejecutar en estos tipados

const myProductStock1 = 100; // las const no pueden reasignar los valores, no se pueden cambiar los datos.

})();


